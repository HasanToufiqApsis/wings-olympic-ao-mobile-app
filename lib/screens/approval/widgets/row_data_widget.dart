

import 'package:flutter/material.dart';

class RowDataWidget extends StatelessWidget {
  const RowDataWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
